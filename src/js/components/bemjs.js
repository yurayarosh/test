import { BEMblock } from '../helpers'

const isActive = 'active'

// document.addEventListener('click', e => {
//   const heroBlock = e.target.closest('button')
//   if (!heroBlock) return

//   BEMblock(heroBlock, 'btn').toggleMod(isActive)
// })
